# Tiny gate KEEP-seed note (2026-09-22)

Flag-off second-process typed gate for Piper T1. Replaces *direction* of same-weights Pulse (NO-KEEP) with a tiny Qwen3 sibling process.

Copied Stage-0 + binary force/nudge policy from Pulse force-binary archaeology (`origin/pulse-force-binary`); decode path is HTTP/stdio to `scripts/lmp_tiny_gate.py`, not in-process `pulse_decode`.

Non-goals: 4-way, fine-tune N=34, Godoer enums, default-on.
