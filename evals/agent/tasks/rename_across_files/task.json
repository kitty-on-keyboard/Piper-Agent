{
  "split": "corpus",
  "mission": "Rename the function `calc_total` to `compute_total` everywhere it appears, including its callers. `python3 -m pytest -q` must still pass afterwards.",
  "mode": "agent",
  "expect": "solved",
  "check": "python3 -m pytest -q && ! grep -rq calc_total . --include='*.py'",
  "max_iterations": 30,
  "wall_clock_seconds": 700,
  "notes": "Multi-file edit with a mechanical ground truth. Measures whether it finds every call site rather than the first.",
  "protect": [
    "test_billing.py"
  ]
}
